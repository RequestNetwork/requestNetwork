/**
 * Log level for the node. Using conventions from https://stackoverflow.com/questions/2031163/when-to-use-the-different-log-levels
 * Only 2 levels today, more levels to be introduced and handled in PROT-501
 */
export enum LogLevel {
  DEBUG = 'debug',
  ERROR = 'error',
}
